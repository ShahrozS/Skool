#spring.application.name=School Database
#
## PostgreSQL connection
#spring.datasource.url=jdbc:postgresql://localhost:5432/school_db
#spring.datasource.username=shahroz
#spring.datasource.password=shahroz
#spring.datasource.driver-class-name=org.postgresql.Driver
#
## JPA
#spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
#spring.jpa.hibernate.ddl-auto=update
#
#server.port=8080



###### FOR DOCKER  #######

spring.application.name=School Database

# Use environment variables passed by docker-compose
spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=${SPRING_JPA_HIBERNATE_DDL_AUTO:update}

# Optional: Swagger/OpenAPI path
springdoc.swagger-ui.path=/swagger-ui.html

# Optional: keep this 8080 (Docker already maps to host)
server.port=8080

